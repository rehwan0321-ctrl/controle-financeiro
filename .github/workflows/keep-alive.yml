name: Keep Supabase Alive

on:
  schedule:
    # Runs every 5 days at 12:00 UTC (project pauses after 7 days)
    - cron: '0 12 */5 * *'
  workflow_dispatch: # Allow manual trigger

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase health endpoint
        run: |
          curl -s "https://qubkmecpxbsdphtmwvvw.supabase.co/rest/v1/" \
            -H "apikey: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InF1YmttZWNweGJzZHBodG13dnZ3Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzQxNDI5NDIsImV4cCI6MjA4OTcxODk0Mn0.Y72dKZFiqCh-CMNLMyi5Yg7lOLGT4BsODQQO0FSD54E" \
            -o /dev/null -w "Status: %{http_code}\n"
          echo "Ping enviado ao Supabase - projeto mantido ativo!"
